class Solution {
public:
    int largestAltitude(vector<int>& gain) {
       int temp =0;
       int res =0;
       for( int i : gain)
       {
           temp+=i;
           res = max(temp,res);
       }
       return res;
    }
};
